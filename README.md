# lip2025

The repository contains three projects:

1) funst contains the big step semantics of the language FUN statically scoped
2) fundyn contains the big step semantics of the language FUN dynamically scoped
3) funtypeinf contains the type inference for the language FUN  
